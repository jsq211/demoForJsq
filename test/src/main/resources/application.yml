
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&&useSSL=false&amp&serverTimezone=GMT%2B8&allowMultiQueries=true
    username: root
    password: admin
  jackson:
    serialization:
      indent_output: true
  redis:
    host: 116.62.204.205 # Redis服务器地址
    database: 1 # Redis数据库索引（默认为0）
    port: 6379 # Redis服务器连接端口
    password: redis@704762133 # Redis服务器连接密码（默认为空）
    timeout: 300ms # 连接超时时间（毫秒）

jsq:
  sync:
    enabled: true
    prefix: ums_